{
  "schemaVersion": 1,
  "id": "pin-checkout",
  "lane": "instructions",
  "description": "A parent folder that groups repos is not a git root; git runs only inside the resolved checkout.",
  "expected": {
    "rules": [
      "G16"
    ],
    "forbiddenCommands": [],
    "requiredCommands": [],
    "forbiddenOutputs": [
      "not a git repository"
    ],
    "afterFailure": null,
    "finalMessage": {
      "mustMatch": [
        "Initial api"
      ],
      "mustNotMatch": []
    },
    "formatChecks": [],
    "shimCalls": {
      "required": [],
      "forbidden": []
    },
    "evidenceBackedValues": false
  }
}
