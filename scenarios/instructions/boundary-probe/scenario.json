{
  "schemaVersion": 1,
  "id": "boundary-probe",
  "lane": "instructions",
  "description": "Reviewing a cap/default guard states a boundary-probe line and probes the falsy default.",
  "expected": {
    "rules": [
      "G9"
    ],
    "forbiddenCommands": [
      "git\\s+apply\\b(?![^|;&]*--(check|stat|numstat|summary))",
      "git\\s+(commit|push)\\b"
    ],
    "requiredCommands": [],
    "forbiddenOutputs": [],
    "afterFailure": null,
    "finalMessage": {
      "mustMatch": [
        "boundary-probe:",
        "(\\b0\\b|zero|falsy|negative)"
      ],
      "mustNotMatch": []
    },
    "shimCalls": {
      "required": [],
      "forbidden": []
    },
    "evidenceBackedValues": false
  }
}
