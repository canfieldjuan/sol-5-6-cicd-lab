# Instruction-retention scenarios

These fixtures are NOT given an AGENTS.md of their own. The runner puts the arm's
global AGENTS.md in an isolated CODEX_HOME, so the instruction text is the only
variable. Keep fixture repos free of AGENTS.md files.
